Project Backlog

Use this backlog to track pending tasks in priority order. The item at the
top should be handled next, while lower items should wait accordingly. Mark
each item with an asterisk followed by a blank line for clarity. Note when
tasks are blocked, and specify what they are blocked on, including related
tasks, bugs, or people. Keep descriptions concise and actionable.

* Implement hyperlink highlighting and cyclic keyboard navigation defined in
  docs/specifications/hyperlink_navigation.txt. Deliver underline plus focus
  styling, TAB/SHIFT+TAB traversal with wraparound, and viewport alignment for
  focused links. Blocked until we select or build a focus-aware renderer in
  place of the current less-based paging pipeline.

* Draft a focused specification for Markdown heading rendering that clarifies
  fixtures, expected styling cues, and compatibility notes. Keep it scoped to
  headings in isolation and store it under docs/specifications/testing/.

* Draft a specification for Markdown bold rendering that captures fixture
  inputs, expected bold tokenization, and failure modes in
  docs/specifications/testing/.

* Draft a specification for Markdown italic rendering that details fixtures,
  styling expectations, and avoidance of spillover into surrounding text.

* Draft a specification for combined bold+italic Markdown rendering that
  defines fixture composition, expected dual-style cues, and boundary handling
  rules.

* Draft a specification for Markdown strikethrough rendering that documents
  fixtures, expected strike styling, and interaction with adjacent emphasis
  markers.

* Draft a specification for inline code rendering that records fixture
  formats, monospace styling expectations, and rules for preserving
  punctuation and spacing.

* Draft a specification for fenced code block rendering that covers fixture
  layout, whitespace preservation, language-hint handling, and fallback
  behavior when syntax highlighting is unavailable.

* Draft a specification for blockquote rendering that sets fixture structure,
  quote styling cues, nesting expectations, and tolerance for interior
  emphasis markers.

* Draft a specification for bulleted list rendering that defines fixtures for
  hyphen and asterisk bullets, list spacing expectations, and separation rules
  between items.

* Draft a specification for ordered list rendering that documents fixtures
  with varied numbering, continuation across lines, and spacing or alignment
  rules.

* Draft a specification for nested list rendering that details parent-child
  indentation fixtures, separation requirements, and safeguards against
  merging levels.

* Draft a specification for horizontal rule rendering that sets fixtures for
  ``---`` and ``***`` markers, expected separator output, and suppression of
  trailing text.

* Draft a specification for Markdown link rendering that outlines inline link
  fixtures, expected link text plus URL surfacing, and renderer-dependent
  metadata handling.

* Draft a specification for Markdown image rendering that captures fixtures
  for alt text and URLs, expected fallbacks when images cannot render, and
  handling of link-adjacent content.

* Draft a specification for Markdown table rendering that establishes simple
  pipe table fixtures, column alignment expectations, and header-versus-body
  distinctions.

* Draft a specification for Markdown escaping that records fixtures with
  backslash-escaped characters, literal rendering expectations, and safeguards
  against unintended formatting.

* Draft a specification for inline HTML passthrough that lists fixtures,
  defines acceptable passthrough behavior, and notes interactions with other
  Markdown elements.

* Build pytest coverage for Markdown heading rendering following the new
  specification. Add fixtures under resources/tests and assert on rendered
  heading cues rather than full snapshots.

* Build pytest coverage for Markdown bold rendering following the dedicated
  specification. Use targeted fixtures under resources/tests to assert on bold
  styling tokens and boundary handling.

* Build pytest coverage for Markdown italic rendering per the dedicated
  specification, focusing on isolated fixtures and assertions that italic
  styling remains contained.

* Build pytest coverage for combined bold+italic rendering according to the
  specification. Cover boundary cases and confirm both styles apply without
  reordering characters.

* Build pytest coverage for Markdown strikethrough rendering per its
  specification. Include fixtures that sit beside other emphasis markers and
  confirm isolated strike styling.

* Build pytest coverage for inline code rendering guided by the specification.
  Add fixtures that stress punctuation and spacing while asserting monospace
  styling cues.

* Build pytest coverage for fenced code block rendering per the specification,
  ensuring whitespace preservation, language-hint tolerance, and renderer
  fallback behavior.

* Build pytest coverage for blockquote rendering following the specification.
  Include nested quote fixtures and assert on indentation or prefix cues.

* Build pytest coverage for bulleted list rendering guided by the
  specification. Cover hyphen and asterisk bullets with assertions on spacing
  and separation.

* Build pytest coverage for ordered list rendering per its specification, with
  fixtures that exercise numbering continuation and spacing rules.

* Build pytest coverage for nested list rendering in line with the
  specification. Use fixtures that differentiate parent and child indentation
  and assert against level merging.

* Build pytest coverage for horizontal rule rendering according to the
  specification. Confirm ``---`` and ``***`` fixtures render separators
  without stray text.

* Build pytest coverage for Markdown link rendering per the specification.
  Assert that link text appears and URLs surface in output or metadata per
  renderer capability.

* Build pytest coverage for Markdown image rendering in accordance with the
  specification. Validate alt text display and safe handling of image URLs
  when rendering is unavailable.

* Build pytest coverage for Markdown table rendering following the
  specification, asserting on column separation cues and header distinction.

* Build pytest coverage for Markdown escaping per the specification. Ensure
  backslash-escaped characters render literally and do not trigger formatting.

* Build pytest coverage for inline HTML passthrough per the specification.
  Confirm inline HTML text remains intact and coexists with surrounding
  Markdown elements.
