Project Backlog

Use this backlog to track pending tasks in priority order. The item at the
top should be handled next, while lower items should wait accordingly. Mark
each item with an asterisk followed by a blank line for clarity. Note when
tasks are blocked, and specify what they are blocked on, including related
tasks, bugs, or people. Keep descriptions concise and actionable.

* Build pytest coverage for Markdown image rendering in accordance with the
  specification. Validate alt text display and safe handling of image URLs
  when rendering is unavailable. Added 2026-03-07T03:04:20Z.

* Build pytest coverage for Markdown table rendering following the
  specification, asserting on column separation cues and header distinction.
  Added 2026-03-07T03:04:20Z.

* Build pytest coverage for Markdown escaping per the specification. Ensure
  backslash-escaped characters render literally and do not trigger formatting.
  Added 2026-03-07T03:04:20Z.

* Draft a specification for vi-style visual mode selection covering V entry,
  arrow-key navigation, spacebar toggling for start/end/clear, Enter-based
  multi-clipboard copy and exit, and Escape exit. Added 2025-12-09T03:13:26Z.

* Implement Windows 11 command-line support per the proposal in
  project-management/proposals/windows-11-cli-support.txt. Ensure cmd.exe and
  PowerShell users have parity with POSIX workflows, including shims,
  documentation, and CI coverage. Added 2025-12-09T05:04:57Z.

* Draft a specification for a ``--flatten-unicode`` option that maps Unicode
  characters to classic 8-bit ASCII fallbacks for rendering, ensuring
  predictable output while preserving intent. Added 2025-12-09T06:20:30Z.

* Draft a specification for a ``--7bit`` option that converts 8-bit ASCII to
  nearest printable 7-bit mappings and defines combined behavior with
  ``--flatten-unicode``. Added 2025-12-09T06:20:30Z.

* Draft a proposal for version-management workflow extensions covering
  multi-version support, including bugs that affect both current and older
  supported maintenance branches. Added 2026-03-06T22:01:06-08:00.
  Priority: low.
  Acceptance: proposal defines branching, backport/cherry-pick policy,
  bug-tracking linkage across versions, and release-note traceability.
