# Known bugs
# ----------
#
# Keep confirmed bugs awaiting work in this file. Use an asterisk followed by
# a blank line for each entry, mirroring `/project-management/backlog.txt`.
# Include a concise summary, reproduction notes when available, the ISO 8601
# timestamp for when the bug was logged, and any blockers or owners.

* Centered items do not recompute positions after window resizes, leaving
  elements misaligned. Resize the window while viewing centered content to see
  the stale layout. Added 2025-12-12T00:53:48Z.

* Header tags no longer render with full-width frames and the frame width does
  not recompute on window resizes, causing wraps onto the next line. Restore
  the full-width frames and recalc widths on resize. Added 2025-12-12T00:53:48Z.
