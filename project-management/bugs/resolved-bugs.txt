Resolved Bugs

Record resolved bugs with newest entries at the top. Use the same
asterisk-plus-blank-line formatting as the active bug files. Each entry must
include the ISO 8601 timestamp for when the fix was recorded, a short symptom
summary, `Root cause:` analysis, and `Resolution:` notes. Add commit or test
references when they help trace the fix.

* 2025-12-12T01:59:17+00:00 Added framebuffer peek/poke helpers for visual
  regression inspection.
  Root cause: The visual regression harness could not inspect or mutate
  viewport cells by terminal coordinate, which left framebuffer-level defects
  hard to reproduce and verify.
  Resolution: Added peek/poke framebuffer helpers so tests can inspect and
  mutate viewport cells deterministically.
  Owner: R. Talon (Matt "Breakpoint" Heck).

* 2025-12-12T03:50:00Z Stabilized the H1 resize regression in padded rows.
  Root cause: The resize path and framebuffer helpers did not preserve width
  and height semantics consistently across resized, padded heading rows.
  Resolution: Made framebuffer helpers width-and-height aware, enforced
  explicit resize dimensions, and extended the resize regression coverage.
  Owner: R. Talon.

* 2025-12-12T01:12:41Z Restored full-width H1 heading frames after resize.
  Root cause: Heading frame widths were not being recomputed correctly against
  current console dimensions after resize events.
  Resolution: Recomputed widths on resize, restored full-width H1 heading
  frames, and expanded resize regression coverage.
  Owner: R. Talon.

* 2025-12-12T00:58:20Z Realigned centered pager content after window resizes.
  Root cause: The pager continued using stale output dimensions after a
  terminal resize, so centered content drifted out of alignment.
  Resolution: Sourced updated widths from the prompt_toolkit output size and
  expanded resize coverage in pager tests.
  Owner: R. Talon.

* 2025-12-11T08:13:05+00:00 Stopped H1 heading panels from wrapping in narrow
  terminals.
  Root cause: Rich panel sizing allowed H1 content to exceed narrow terminal
  widths, which forced unintended wraps.
  Resolution: Constrained panel width to content width and added a narrow-
  console regression test.
  Owner: R. Talon.

* 2025-12-11T07:29:12+00:00 Resolved the editable install metadata failure.
  Root cause: Project metadata omitted the required author email, which broke
  editable install validation.
  Resolution: Populated the project author email and added metadata coverage
  to prevent regression.
  Owner: R. Talon.
  Notes: install verification blocked by outbound proxy restrictions;
  compile-only check succeeded.
