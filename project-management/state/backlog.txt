Project Backlog

Use this backlog to track pending tasks in priority order. The item at the
top should be handled next, while lower items should wait accordingly. Mark
each item with an asterisk followed by a blank line for clarity. Note when
tasks are blocked, and specify what they are blocked on, including related
tasks, bugs, or people. Keep descriptions concise and actionable.

*

  Bring mdview's bootstrap, prerequisite installation, dev-setup tooling,
  and setup-path documentation into compliance with current TheKnowledge
  managed starter expectations, and raise installer/source documentation
  quality to the newer standard.
  Focus first on setup/prerequisite/devsetup/installer work; record any
  approval-requiring or environment-specific follow-ups separately instead of
  blocking this branch.
  Current status: the branch has now adopted TheKnowledge's newer `install.sh`
  plus `scripts/install-stage-2.py` model, restored mdview-local runtime
  policy overrides, added a repo-local `scripts/install_project.py` hook for
  launcher behavior, and updated setup-path documentation. Follow-up work
  should extend the higher-documentation standard into the broader codebase
  after deferred validation is complete.
  Requested by operator. Added: 2026-03-31T18:54:35-07:00.
  Updated: 2026-04-01T10:19:04-07:00.

*

  Add stdin support so mdview can render Markdown piped from another command,
  including workflows like `realitycheck --format md | mdview`.
  Requested by operator. Added: 2026-04-01T20:48:49-07:00.

*

  [LOW][BLOCKED] Implement and validate the mirrored cross-platform strategy
  for running Linux test workflows through WSL2 when development happens on
  Windows.
  Specification and documentation now exist at:
  `docs/specifications/testing/linux_validation_from_windows_wsl2.txt` and
  `docs/testing/wsl2/`.
  Blocked on: execution from a real Windows host with WSL2. Do not attempt
  implementation testing from non-Windows hosts.
  Requested by operator. Added: 2026-03-07T22:06:48-08:00. Updated:
  2026-03-08T03:31:02-07:00.

*

  [LOW][BLOCKED] Review the active TheKnowledge `black` issues after the
  upstream fix lands and remove any mdview-side accommodations if they become
  unnecessary.
  The current mdview integration did not commit a repo-side `black`
  workaround, but this follow-up should confirm that remains true after the
  active upstream work in another terminal is resolved. Blocked on: operator
  or upstream maintainer confirmation that the active TheKnowledge `black`
  fixes are complete. Requested by operator. Added:
  2026-03-27T01:49:48-07:00.
