Completed Tasks

Track finished tasks here with the most recently completed item at the
top and older items below. Mark each entry with an asterisk followed by a
blank line. When closing out work, include brief context such as dates,
responsible contributors, and any follow-up notes. If a task was blocked,
summarize how the blocker was resolved.

* Added Ubersight long-duration work tracking, recorded at
  2026-06-13T22:09:12-07:00 by Codex from direct operator request. Added
  `docs/specifications/ubersight_integration.txt`, mdview-specific
  Ubersight standing orders in `AGENTS.md`,
  `project-management/development-roadmap.txt`, and
  `project-management/state/phase-slice-stack.txt`. Wrote live dashboard
  state to `.local/ubersight/status.json` without committing local status.
  Validation: 78-column wrap check, `entropy_check`, and
  `entropy_tripwire_verify` passed.

* Fixed managed install packaging-tool feedback, recorded at
  2026-05-04T17:39:21-07:00 by Codex from direct operator question. Updated
  `scripts/install-stage-2.py` so standard install virtual environments
  upgrade `pip` along with `setuptools` and `wheel`, and print the exact
  managed-venv Python command operators can rerun manually. Updated
  `docs/specifications/prerequisite_installation.txt` and added regression
  coverage in `tests/test_install_stage_2.py`. Validation: `black`, `ruff`,
  `compileall`, `entropy_check`, `entropy_tripwire_verify`, full `pytest`
  (`385 passed`), and approved out-of-sandbox `demo_check` passed
  (`validated=0 skipped=1 errors=0`).

* Pinned mdview runtime, interactive, and development support dependencies in
  tracked package metadata, recorded at 2026-05-04T17:23:04-07:00 by Codex
  from direct operator instruction. Updated
  `docs/specifications/support_package_currency.txt`, pinned
  `prompt_toolkit==3.0.52`, `rich==15.0.0`, `black==26.3.1`,
  `pytest==9.0.3`, `pytest-timeout==2.4.0`, and `ruff==0.15.12` in
  `pyproject.toml`, and refreshed tracked egg-info metadata. Added a
  `py39` Black target and a Codex-sandbox serial Black fallback so the
  current pinned formatter remains usable by the standard timeout wrapper.
  Validation: `black`, `ruff`, `compileall`, `entropy_check`,
  `entropy_tripwire_verify`, full `pytest` (`384 passed`), and approved
  out-of-sandbox `demo_check` passed (`validated=0 skipped=1 errors=0`).
  Local Windows shim validation remains deferred per
  `.local/windows-shim-validation-note.txt`.

* Refreshed Python support package pins and the repository packaging
  toolchain, recorded at 2026-05-04T15:46:47-07:00 by Codex from direct
  operator instruction. Added
  `docs/specifications/support_package_currency.txt`; updated
  `requirements-dev.txt` from `ruff==0.15.7` to `ruff==0.15.12` and
  `pytest==9.0.2` to `pytest==9.0.3`; upgraded the active `.venv` packaging
  toolchain to `pip==26.1.1`, `setuptools==82.0.1`, and `wheel==0.47.0`;
  and confirmed `pip list --outdated` returned no outdated Python packages.
  Codex npm packages were intentionally left unchanged for codex-wrangler.
  Validation: `black`, `ruff`, `compileall`, `entropy_check`,
  `entropy_tripwire_verify`, full `pytest` (`381 passed`), and `demo_check`
  passed. Local Windows shim validation remains deferred per
  `.local/windows-shim-validation-note.txt`.

* Normalized project-management state after the stdin pipe-target work,
  recorded at 2026-04-02T16:53:05-07:00 by Codex (GPT-5.4) from direct
  operator instruction. Removed the duplicated installer/bootstrap overhaul
  entry from `project-management/state/tasks-in-progress.txt` so that active
  work now aligns with the backlog and the separate pending operator
  validation request in `project-management/state/ai-human-requests.txt`.

* Reworked buffered stdin handling so mdview behaves as a classical pipe
  target, recorded at 2026-04-02T16:39:27-07:00 by Codex (GPT-5.4) from
  direct operator instruction. Added
  `docs/specifications/stdin_buffered_piped_input.txt`; updated README,
  `docs/specifications/project_overview.txt`, the shared POSIX help text,
  Linux/macOS man pages, and tracked package metadata; added
  `src/mdview/input_formats.py`; updated `src/mdview/cli.py` to buffer
  non-interactive stdin with a configurable 16 MB default safety ceiling,
  2-second default idle timeout, startup status lines, and conservative
  Markdown-versus-plain-text detection; updated `src/mdview/rendering.py` so
  the interactive pager prefers the controlling TTY for input after stdin has
  already been consumed; and added regression coverage in
  `tests/test_cli.py`, `tests/test_input_formats.py`, and
  `tests/test_rendering.py`. Validation: `black`, `ruff`, `compileall`,
  `entropy_check`, `entropy_tripwire_verify`, full `pytest`, and `demo_check`
  all passed (`381 passed`; `demo_check` validated=1 skipped=0 errors=0).

* Added boxed Markdown table borders with gray border styling plus
  `--no-table-borders` and `--no-cell-borders`, recorded at
  2026-03-30T15:15:23-07:00 by Codex (GPT-5.4) from direct operator
  instruction. Updated
  `docs/specifications/markdown_table_rendering.txt`,
  `docs/specifications/project_overview.txt`, README, and deployment
  help/man text; extended `src/mdview/cli.py` with the new border flags; and
  updated `src/mdview/rendering.py` so Markdown pipe tables always render
  through mdview's custom boxed-table path, including gray border glyphs,
  span-color preservation, and whitespace-gap fallbacks when border layers
  are disabled. Added regression coverage in `tests/test_cli.py`,
  `tests/test_markdown_tables.py`, and `tests/test_rendering.py` for boxed
  output, border toggles, ANSI border styling, and consistent heading-to-
  table spacing. Validation: approved out-of-sandbox wrapper-based `black`
  passed after reformatting the touched files; wrapper-based `ruff`,
  `compileall`, `entropy_check`, `entropy_tripwire_verify`, and full
  `pytest` all passed (`350 passed`); and approved out-of-sandbox
  `demo_check` passed with the expected cache hit (`validated=0 skipped=1
  errors=0`).

* Polished `demos/table-demo.md` so its Markdown source table is aligned
  cleanly without sacrificing the nicer Rich-rendered table appearance,
  recorded at 2026-03-30T15:03:38-07:00 by Codex (GPT-5.4) from direct
  operator feedback. Tightened the source column spacing, kept the file
  within the 78-column wrap rule, and preserved the span-color showcase by
  using the Rich-friendly divider form that avoids mdview's literal-pipe
  fallback for that demo. Validation: direct `render_to_ansi` inspection
  confirmed the rendered table returned to aligned Rich table output, and the
  approved out-of-sandbox `demo_check` run passed (`validated=1 skipped=0
  errors=0`).

* Added a cached demo-document workflow with a new Markdown showcase at
  `demos/table-demo.md`, recorded at 2026-03-30T10:33:46-07:00 by Codex
  (GPT-5.4) from direct operator instruction. Added
  `docs/specifications/demo_documents.txt`; added
  `scripts/validate_demos.py` plus timeout-wrapper and cached quality-gate
  integration for `demo_check`; updated workflow guidance in `AGENTS.md`,
  `docs/development-workflow.txt`, `docs/running-tests.txt`,
  `project-management/git-flow.txt`, README, and
  `docs/specifications/project_overview.txt`; and added regression coverage
  in `tests/test_validate_demos.py`, `tests/test_run_quality_gate_cached.py`,
  and `tests/test_repository_setup.py`. Validation: targeted `pytest` on
  the new validator and workflow-doc tests passed; wrapper-based `black`,
  approved out-of-sandbox `demo_check`, wrapper-based `ruff`, `compileall`,
  `entropy_check`, `entropy_tripwire_verify`, and full `pytest` all passed
  (`347 passed`). Note: the live Codex sandbox could not write the `.git`
  demo-cache path, so the successful cache-writing `demo_check` runs used the
  approved out-of-sandbox path for this session.

* Restored Markdown `span` color attributes in rendered output and added
  interactive `!` framebuffer capture support, recorded at
  2026-03-27T10:49:02-07:00 by Codex (GPT-5.4) from direct operator
  instruction. Added
  `docs/specifications/manual_screen_capture_hotkey.txt`; updated
  `docs/specifications/markdown_table_rendering.txt`,
  `docs/specifications/project_overview.txt`, README, and deployment help/man
  text; added `--screen-dump-dir` plumbing in `src/mdview/cli.py`; updated
  `src/mdview/rendering.py` so supported HTML `span` color wrappers emit ANSI
  foreground styling without disturbing table width calculations or
  heading-adjacent table spacing; and added regression coverage in
  `tests/test_cli.py`, `tests/test_markdown_tables.py`, and
  `tests/test_rendering.py` for manual `!` capture artifacts plus JSON
  foreground attributes on colored table cells. Validation: targeted
  `pytest` on `tests/test_markdown_tables.py`, `tests/test_rendering.py`, and
  `tests/test_cli.py` passed; approved out-of-sandbox wrapper-based `black`
  passed after the in-sandbox formatter path remained unsuitable; and
  wrapper-based `ruff`, `compileall`, `entropy_check`,
  `entropy_tripwire_verify`, and full `pytest` all passed (`338 passed`).

* Fixed the heading-adjacent Markdown table spacing regression for table
  cells wrapped in HTML `span` color tags, recorded at
  2026-03-27T10:15:15-07:00 by Codex (GPT-5.4) from direct operator
  instruction. Updated `docs/specifications/markdown_table_rendering.txt`
  to require equal heading-to-table spacing for plain and span-wrapped
  tables; added fixtures under `resources/tests/markdown/tables/`; extended
  `tests/test_markdown_tables.py` with a formatter regression and a
  framebuffer-based visual comparison; and updated `src/mdview/rendering.py`
  so pipe-table cell parsing strips inline `span` wrappers while preserving
  the visible cell text, preventing those tags from perturbing Rich's table
  layout. Validation: targeted `pytest` on
  `tests/test_markdown_tables.py` and `tests/test_markdown_headings.py`
  passed, approved out-of-sandbox wrapper-based `black` passed after the
  in-sandbox formatter path repeated the known hang pattern, and wrapper-
  based `ruff`, `compileall`, `entropy_check`,
  `entropy_tripwire_verify`, and full `pytest` all passed (`333 passed`).

* Integrated `TheKnowledge` as a root consumer submodule and wired mdview's
  local bridge workflow, recorded at 2026-03-27T01:49:48-07:00 by Codex
  (GPT-5.4) from direct operator instruction. Added `.gitmodules` and the
  `TheKnowledge/` submodule via the configured `github-hpg` SSH alias; added
  `docs/specifications/theknowledge_consumer_repository_integration.txt`;
  moved the older overlay-layout proposal into
  `project-management/state/proposals/superseded/`; wrapped `AGENTS.md`
  with managed TheKnowledge header/footer sections plus mdview-specific
  local overrides; added `scripts/_theknowledge_delegate.py`,
  `scripts/report_managed_agents_drift.py`, and
  `scripts/refresh_managed_agents.py`; updated workflow, entropy, and
  timeout-wrapper configuration so default `pytest` discovery stays scoped to
  mdview's `tests/` tree; excluded `TheKnowledge/` from entropy scanning,
  tripwire verification, and cached repo-root entropy fingerprints; and
  added regression coverage in `tests/test_repository_setup.py`,
  `tests/test_entropy_harness.py`, `tests/test_run_quality_gate_cached.py`,
  and `tests/test_verify_entropy_tripwire.py`. Follow-up: backlogged a
  blocked review of upstream TheKnowledge `black` issues per operator
  direction; no repo-side temporary `black` workaround was committed in
  mdview. Validation: `black` passed outside the Codex sandbox via the
  repo-local `.venv` because sandboxed `black` continued to hang; `ruff`,
  `compileall`, `entropy_check`, `entropy_tripwire_verify`, and full
  `pytest` all passed (`331 passed`).

* Merged the accumulated `VERSIONS/0.5.0` work back into `trunk` and retired
  the version-branch experiment, recorded at 2026-03-26T12:58:39-07:00 by
  Codex (GPT-5.4) from direct operator instruction. Resolved trunk merge
  conflicts across prerequisite/bootstrap docs and scripts, state-file
  layout, and generated metadata; preserved the newer
  `project-management/state/...` structure while carrying forward the
  2026-03-13 trunk-only completed-task history and open BUG-2026-03-13-01;
  updated `project-management/git-flow.txt`, `docs/development-workflow.txt`,
  and `AGENTS.md` to restore trunk-first policy and prohibit routine
  `VERSIONS/*` work unless operators explicitly reinstate it; moved
  `version-management-multi-version-support.txt` into
  `project-management/state/proposals/rejected/`; and taught the entropy
  scanner, verifier, and cached quality gate to exclude repo-root
  `package-lock.json` integrity hashes, with regression coverage in
  `tests/test_entropy_harness.py`, `tests/test_verify_entropy_tripwire.py`,
  and `tests/test_run_quality_gate_cached.py`. Validation: wrapper-based
  `black`, `ruff`, `compileall`, `entropy_check`,
  `entropy_tripwire_verify`, and full `pytest` all passed (`322 passed`);
  `python scripts/run_quality_gate_cached.py` passed; and
  `windows_shims_linux` returned `SKIP` because Podman was unavailable in the
  current permission context and host `wine` was not installed, so Windows CI
  remains the compatibility gate.

* Aligned the branch with the project-local Codex takeover environment,
  recorded at 2026-03-26T12:13:06-07:00 by Codex (GPT-5.4) from direct
  operator instruction. Updated
  `docs/specifications/quality_gate_black_scope_and_exclusions.txt` and
  `scripts/tool_timeouts.json` so the required `black` command remains scoped
  to repository-owned Python roots without relying on an unsupported flag in
  the live formatter environment; extended
  `docs/specifications/security_entropy_tripwire_git_workflow.txt` and
  `docs/security/entropy-checks.txt` so repo-local Codex state is treated as
  generated local environment data; updated
  `dev-utils/security/entropy-check.py` and
  `dev-utils/security/verify_entropy_tripwire.py` plus
  `scripts/run_quality_gate_cached.py` to exclude `.codex-home`,
  `.codex-local`, `bin/codex-local`, and
  `README-LOCAL-Start-Codex.md`; added regression coverage in
  `tests/test_entropy_harness.py`,
  `tests/test_run_quality_gate_cached.py`, and
  `tests/test_verify_entropy_tripwire.py`; fixed a segmented-render assertion
  in `tests/test_rendering.py`; and recorded BUG-2026-03-26-01 in
  `project-management/state/bugs/closed-bugs.txt` with a resolved report.
  Validation: wrapper-based `black` (using the approved out-of-sandbox path
  for this Codex session), `ruff`, `compileall`, `entropy_check`,
  `entropy_tripwire_verify`, `scripts/run_quality_gate_cached.py --checks
  entropy_tripwire_verify`, and full `pytest` all passed (`306 passed`).

* Installed the resolved-bugs guidance and working copy derived from
  TheKnowledge. Recorded at 2026-03-13T09:08:05Z by Codex (GPT-5) from
  direct operator instruction. Updated AGENTS.md and
  `docs/human-orders-for-AI-agents.txt` so bug closures targeted the
  resolved-bug log used at that time, migrated the historical resolved bug
  records accordingly, and returned BUG-2026-03-13-01 from bugs-in-progress
  to known-bugs after the operator redirected work before remediation
  completed. Validation: 78-column wrap checks passed for the updated mdview
  text files.

* Added installer prompting for arbitrary-directory `mdview` resolution and
  PATH detection reporting. Recorded at 2026-03-13T08:04:46Z by Codex
  (GPT-5) from direct operator instruction. Updated the prerequisite
  installation specification, installation and workflow docs, and AGENTS
  guidance so interactive bootstrap runs report whether another `mdview`
  command appears on PATH before asking whether the checkout-local copy
  should win. Added `--mdview-command {prompt,local,system}` handling in
  `scripts/install_prerequisites.py`, managed user-local shim
  creation/removal at `~/.local/bin/mdview`, unmanaged-command protection,
  and shell-wrapper support for forwarding `--` arguments. Expanded
  `tests/test_install_prerequisites.py` to cover PATH probing, prompt
  messaging, and shim lifecycle behavior. Validation:
  `.venv/bin/python scripts/run_tool_with_timeout.py ruff --`
  `tests/test_install_prerequisites.py scripts/install_prerequisites.py`
  passed, `.venv/bin/python scripts/run_tool_with_timeout.py compileall`
  passed, `.venv/bin/python scripts/run_tool_with_timeout.py pytest --`
  `tests/test_install_prerequisites.py tests/test_project_metadata.py`
  `tests/test_run_tool_with_timeout.py tests/test_prerequisites_module.py`
  passed, and `.venv/bin/python scripts/run_tool_with_timeout.py pytest`
  still reports only the known 18 pre-existing failures tracked in the bug
  list. Follow-up: repository-wide black still hangs through the timeout
  wrapper, consistent with BUG-2026-03-13-01.

* Added checkout-local bootstrap guidance and enforced mandatory local-check
  tool installation. Recorded at 2026-03-13T07:41:13Z by Codex (GPT-5) from
  direct operator instruction. Updated AGENTS.md plus workflow,
  installation, sandbox, and test-running docs to require
  `./scripts/install_prerequisites.sh` and `.venv/bin/python` (or an
  activated `.venv`) for local checks; fixed prerequisite warnings to
  reference the canonical installer; updated `pyproject.toml` so bootstrap
  installs `prompt_toolkit` plus the mandatory `black`, `pytest`, and
  `ruff` dev tools; taught `scripts/install_prerequisites.py` to skip
  package-manager work when the required system tools already exist; and made
  `scripts/run_tool_with_timeout.py` fail fast when mandatory tools are
  missing from the active interpreter. Added regression coverage in
  `tests/test_install_prerequisites.py`, `tests/test_project_metadata.py`,
  and `tests/test_run_tool_with_timeout.py`. Validation:
  `./scripts/install_prerequisites.sh` completed after network approval and
  skipped unnecessary system package installation,
  `.venv/bin/python scripts/run_tool_with_timeout.py ruff` passed,
  `.venv/bin/python scripts/run_tool_with_timeout.py compileall` passed,
  `.venv/bin/python scripts/run_tool_with_timeout.py pytest --`
  `tests/test_install_prerequisites.py tests/test_prerequisites_module.py`
  `tests/test_project_metadata.py tests/test_run_tool_with_timeout.py`
  `tests/test_cli.py` passed, and
  `.venv/bin/python scripts/run_tool_with_timeout.py pytest` still reports
  the known 18 pre-existing failures tracked in the bug list. Follow-up:
  filed BUG-2026-03-13-01 for the unresolved repository-wide `black` hang.

* Strengthened documentation/comment standards and resolved the quality-gate
  `black` timeout, recorded at 2026-03-10T08:13:30-07:00 by Codex (GPT-5.4)
  from direct operator instruction. Updated
  `docs/development-workflow.txt`, `project-management/git-flow.txt`, and
  `docs/specifications/documentation_standard_profile.txt`; expanded
  explanatory maintenance comments in `src/mdview/cli.py`,
  `src/mdview/input_feedback.py`, and `src/mdview/rendering.py`; added
  `docs/specifications/quality_gate_black_scope_and_exclusions.txt`; changed
  `scripts/tool_timeouts.json` so `black` formats only repository-owned Python
  roots (`src`, `tests`, `scripts`, `dev-utils`) instead of recursively
  scanning from repository root with an unsafe exclude pattern; added
  regression coverage in `tests/test_entropy_harness.py`; and created
  `project-management/state/bugs/reports/` with a resolved issue report for
  BUG-2026-03-10-01. Validation: wrapper-based `black`, `ruff`, `compileall`,
  `entropy_check`, `entropy_tripwire_verify`, and full `pytest` all passed
  (`304 passed`).

* Stabilized Markdown wrapping after list-boundary forced breaks, recorded at
  2026-03-10T02:26:54-07:00 by Codex (GPT-5.4) from direct operator
  instruction. Added
  `docs/specifications/markdown_forced_break_wrap_stability.txt`; updated
  `src/mdview/rendering.py` to use separate Rich break markers for structural
  list/heading transitions versus prose-after-list spacing so hidden break
  markers no longer contribute visible wrap width to paragraphs; added the
  fixture
  `resources/tests/markdown/lists/bulleted_followed_by_bold_paragraph.md`;
  and extended `tests/test_markdown_bulleted_lists.py` with a width-62
  regression that keeps `This repository does not include` on one line
  after a list block. Validation: wrapper-based black, ruff, compileall,
  entropy_check, entropy_tripwire_verify, and full pytest all passed
  (`303 passed`). Manual reproduction against `../TheKnowledge/README.md`
  confirmed the previous failure band around widths 62-66 and verified
  that the fixed renderer no longer inserts the bad break there.

* Fixed live prompt_toolkit redraw-check-digit drift and non-scrolling
  vertical navigation, recorded at 2026-03-10T01:21:05-07:00 by Codex
  (GPT-5.4) from direct operator instruction. Updated
  `docs/specifications/redraw_check_digit_overlay.txt` to require a
  viewport-relative overlay layer and a live render-loop regression; updated
  `src/mdview/rendering.py` to anchor the digit as a screen-relative
  prompt_toolkit float while routing viewport movement through supported
  scroll callbacks and a hidden cursor position so `up`/`down` navigation
  actually scrolls in real terminals; kept `src/mdview/input_feedback.py`
  compatible with the expanded prompt_toolkit component tuple; and added a
  live prompt_toolkit regression plus prompt_toolkit-availability-robust test
  assertions in `tests/test_rendering.py`. Validation: wrapper-based black,
  ruff, compileall, entropy_check, entropy_tripwire_verify, and full pytest
  all passed (`302 passed`), and a launcher-level PTY smoke run of
  `./mdview README.md --redraw-check-digit ... --automation-json '[[0.0,
  \"down\"]]'` showed the viewport advancing on the first `down` key while
  the digit remained centered.

* Strengthened redraw-check-digit regression coverage for repeated vertical
  scrolling, recorded at 2026-03-09T15:56:03-07:00 by Codex (GPT-5.4) from
  direct operator instruction. Updated
  `docs/specifications/redraw_check_digit_overlay.txt` to require repeated
  vertical-scroll stability coverage, and added a prompt_toolkit stress test
  in `tests/test_rendering.py` that runs five cycles of ten `up` presses
  followed by ten `down` presses, asserting that every redraw advances the
  digit modulo 10 and keeps it at the same absolute viewport cell after
  cropping. Validation: wrapper-based black, ruff, compileall,
  entropy_check, entropy_tripwire_verify, and full pytest all passed
  (`301 passed`).

* Added an optional interactive redraw check-digit overlay for viewport
  debugging, recorded at 2026-03-09T15:50:36-07:00 by Codex (GPT-5.4) from
  direct operator instruction. Added specification
  `docs/specifications/redraw_check_digit_overlay.txt`; added
  `--redraw-check-digit` in `src/mdview/cli.py`; updated
  `src/mdview/rendering.py` so prompt_toolkit redraws overlay a center-screen
  digit that advances modulo 10 at the formatted-text redraw boundary; added
  regression coverage in `tests/test_cli.py` and `tests/test_rendering.py`;
  and updated README, shared POSIX help text, Linux and macOS man pages,
  `docs/specifications/project_overview.txt`, and
  `docs/specifications/fallback-behavior.txt`.
  Validation: wrapper-based black, ruff, compileall, entropy_check,
  entropy_tripwire_verify, and full pytest all passed (`300 passed`).

* Added standalone `--test-input-feedback` diagnostics for arrow-key
  troubleshooting, recorded at 2026-03-09T15:36:22-07:00 by Codex
  (GPT-5.4) from direct operator instruction. Added specification
  `docs/specifications/test_input_feedback_mode.txt`; implemented
  `src/mdview/input_feedback.py` with a line-oriented direct-input stage and
  a full-screen prompt_toolkit stage, both supporting Left/Right arrows plus
  `,` and `.` alternates with timestamped debug logging of input and redraw
  events; wired the standalone mode through `src/mdview/cli.py`; added
  regression coverage in `tests/test_input_feedback.py` and
  `tests/test_cli.py`; and updated README, shared POSIX help text, Linux and
  macOS man pages, and `docs/specifications/project_overview.txt`.
  Validation: wrapper-based black, ruff, compileall, entropy_check,
  entropy_tripwire_verify, and full pytest all passed (`295 passed`).

* Fixed viewport movement key handling after operator report that arrow keys
  were not reliably moving the document. Recorded at
  2026-03-08T21:33:02-07:00 by Codex (GPT-5) from direct operator
  instruction. Updated `src/mdview/rendering.py` to avoid `Esc` as a quit
  binding (preventing escape-sequence conflicts), broadened movement bindings
  to include modified arrow/page keys, and hardened zero-delay automation
  replay startup scheduling so `--automation-json '[[0.0, ...]]'` no longer
  raises event-loop errors and is applied before timeout captures. Tuned
  prompt_toolkit escape-sequence flush timing (`ttimeoutlen`/`timeoutlen`)
  for high-latency terminal links so arrow-key sequences are less likely to
  fragment into non-navigation keypresses. Added regression coverage in
  `tests/test_rendering.py`, including modified key bindings and zero-delay
  replay behavior when `call_from_executor` reports no running loop.
  Validation: wrapper-based black, ruff, compileall, entropy_check,
  entropy_tripwire_verify, and full pytest all passed (`285 passed`).

* Implemented viewport-only navigation controls and `--automation-json`
  replay for deterministic viewer movement testing. Recorded at
  2026-03-08T20:34:56-07:00 by Codex (GPT-5) from direct operator
  instruction. Added specification
  `docs/specifications/viewport_navigation_and_automation_json.txt` and
  user documentation at `docs/movement.txt`; updated README, POSIX help, and
  Linux/macOS man pages; implemented JSON source parsing and validation in
  `src/mdview/cli.py`; implemented timed key replay parsing/injection in
  `src/mdview/rendering.py`; and added regression coverage in
  `tests/test_cli.py` and `tests/test_rendering.py`, including baseline
  versus replayed viewport timeout-capture assertions.
  Validation: wrapper-based black, ruff, compileall, entropy_check,
  entropy_tripwire_verify, and full pytest all passed (`283 passed`).

* Fixed first-frame Markdown heading centering in interactive sessions by
  rendering the initial document with active viewport width. Recorded at
  2026-03-08T19:33:45-07:00 by Codex (GPT-5) from operator-reported Termius
  behavior. Updated `src/mdview/cli.py` to detect startup width from
  `--viewport-columns` or terminal columns before initial render, and added
  regression coverage in `tests/test_cli.py` for terminal-width startup and
  viewport-override precedence. Updated
  `docs/specifications/testing/markdown_heading_rendering.txt` with an
  explicit first-frame width requirement. Validation: wrapper-based black,
  ruff, compileall, entropy_check, entropy_tripwire_verify, and full pytest
  all passed (`273 passed`).

* Replaced timeout-capture artifact format with basename-driven dual outputs
  for deterministic text and style regression workflows. Recorded at
  2026-03-08T18:49:12-07:00 by Codex (GPT-5) from direct operator
  instruction. Added proposal
  `project-management/state/proposals/completed/`
  `automation-timeout-dual-artifact-text-and-attrs-capture.txt`,
  updated specification
  `docs/specifications/automation_timeout_option.txt`, changed
  `--automation-timeout-screenshot` semantics to basename output in
  `src/mdview/cli.py`, and implemented paired artifact capture in
  `src/mdview/rendering.py` as `<basename>.txt` (ASCII, CR row endings) plus
  `<basename>.attrs.json` (per-cell UTF-8 character and style metadata).
  Updated docs/help/manpages and sandbox guidance, including
  `docs/testing/sandbox-synthetic-viewport-and-framebuffer-capture.txt`, and
  expanded regression coverage in `tests/test_cli.py` and
  `tests/test_rendering.py`. Validation: wrapper-based black, ruff,
  compileall, entropy_check, entropy_tripwire_verify, and full pytest all
  passed (`271 passed`).

* Implemented automation-timeout framebuffer screenshot capture with
  deterministic synthetic viewport controls. Recorded at
  2026-03-08T17:57:07-07:00 by Codex (GPT-5) from direct operator
  instruction. Added proposal
  `project-management/state/proposals/completed/`
  `automation-timeout-framebuffer-screenshots-and-`
  `synthetic-viewport-controls.txt`,
  expanded specification
  `docs/specifications/automation_timeout_option.txt`, implemented new CLI
  options (`--automation-timeout-screenshot`, `--viewport-columns`,
  `--viewport-rows`) in `src/mdview/cli.py`, added timeout-exit viewport
  framebuffer artifact capture and viewport override handling in
  `src/mdview/rendering.py`, updated CLI/manpage/user docs including
  `docs/testing/sandbox-synthetic-viewport-and-framebuffer-capture.txt`, and
  expanded coverage in `tests/test_cli.py` and `tests/test_rendering.py`.
  Validation: wrapper-based black, ruff, compileall, entropy_check,
  entropy_tripwire_verify, and full pytest all passed (`271 passed`).

* Implemented multi-document CLI viewing with next/previous navigation and
  Monkey-in-the-Loop telemetry mode. Recorded at 2026-03-08T06:59:47-07:00
  by Codex (GPT-5) from direct operator instruction. Added specification
  `docs/specifications/multi_document_navigation_and_mil.txt`, implemented
  multi-path loading and per-document policy rendering in `src/mdview/cli.py`,
  added `n`/`p` document switching and UI event telemetry hooks in
  `src/mdview/rendering.py`, introduced `--verbose` switch reporting and
  `--MIL`/`--mil` action logging, updated docs including
  `docs/testing/Monkey-In-the-Loop.txt` and canonical POSIX help text, and
  expanded coverage in `tests/test_cli.py` and `tests/test_rendering.py`.
  Validation: wrapper-based black, ruff, compileall, entropy_check,
  entropy_tripwire_verify, and full pytest all passed (`255 passed`).

* Replaced ad hoc interactive corpus smoke commands with a reusable PTY
  harness and completed a documented 200-file run. Recorded at
  2026-03-08T06:41:04-07:00 by Codex (GPT-5) from direct operator
  instruction. Added `dev-utils/smoke/interactive_markdown_smoke.py`,
  documented workflow and maintenance in
  `docs/testing/interactive-markdown-smoke.txt`, added timeout-wrapper key
  `interactive_markdown_smoke`, and updated `AGENTS.md` plus
  `docs/development-workflow.txt` to require reusable smoke workflows. Ran
  `python dev-utils/smoke/interactive_markdown_smoke.py --sample-count 200
  --progress-interval 25 --seed 20260308` with unrestricted PTY access:
  first-pass failures `0`, persistent failures `0`, output bundle
  `/tmp/mdview-random-md-smoke-20260308-064005/`.

* Added proposal hold-area governance for debated architecture work.
  Recorded at 2026-03-08T04:09:42-07:00 by Codex (GPT-5) from direct
  operator instruction. Created
  `project-management/state/proposals/under-review/`, moved
  `content-element-style-hierarchy-plan.txt` into that directory, and updated
  `AGENTS.md` with a standing rule that AI agents must not auto-implement
  proposals in `under-review` unless the operator explicitly moves them out.

* Implemented adaptive two-profile table width strategy with a
  readability-first CLI override. Recorded at 2026-03-08T03:52:58-07:00 by
  Codex (GPT-5) from direct operator instruction. Added specification
  `docs/specifications/table_width_profile_selection.txt`, updated table and
  overview specs, added `--readability-first-tables` in `src/mdview/cli.py`,
  and implemented fit-first/readability-first profile selection in
  `src/mdview/rendering.py` for Markdown table formatting. Added coverage in
  `tests/test_markdown_tables.py` and updated CLI help assertions in
  `tests/test_cli.py`. Validation: wrapper-based black, ruff, compileall,
  entropy_check, entropy_tripwire_verify, and pytest all passed
  (`251 passed`).

* Drafted the WSL2 mirrored Linux-validation specification and operator
  documentation set; deferred execution/implementation testing to real
  Windows hosts only. Recorded at 2026-03-08T03:31:02-07:00 by Codex (GPT-5)
  from direct operator instruction. Added
  `docs/specifications/testing/linux_validation_from_windows_wsl2.txt` and
  `docs/testing/wsl2/{README,maintenance,troubleshooting}.txt`, and updated
  AGENTS and test/deployment docs with explicit non-Windows-host execution
  restrictions and CI authority boundaries.

* Closed six hardening gaps in entropy-tripwire and push-gate automation, and
  added parse-stable JSON scanner output. Recorded at
  2026-03-08T03:02:01-07:00 by Codex (GPT-5) from direct operator
  instruction. Added `--json-output` with schema key ordering/versioning in
  `dev-utils/security/entropy-check.py`, migrated
  `verify_entropy_tripwire.py` to JSON parsing, hardened
  `scripts/install_git_hooks.py` with wrapper-based preservation for existing
  unmanaged hooks plus managed `pre-push`, and completed cache-backed quality
  gating via `scripts/run_quality_gate_cached.py` and
  `scripts/git_standard_commit_push.py`. Added and updated regression tests
  for JSON output, hook wrapping, dry-run behavior, and cache invalidation.
  Validation: wrapper-based `black`, `ruff`, `compileall`, `entropy_check`,
  `entropy_tripwire_verify`, and `pytest` all passed (`247 passed`).

* Added standardized git commit/push and veteran-pull utilities with
  mandatory entropy tripwire push gating. Recorded at
  2026-03-08T01:14:43-08:00 by Codex (GPT-5) from direct operator
  instruction. Added `scripts/git_standard_commit_push.py` and
  `scripts/git_veteran_pull.py`, plus tripwire verifier
  `dev-utils/security/verify_entropy_tripwire.py`. Push gating now enforces:
  sentinel detection, clean harness scan after sentinel exclusion, and
  independent scan-count verification. Added timeout-wrapper key
  `entropy_tripwire_verify`, expanded docs in
  `docs/security/entropy-checks.txt`,
  `docs/security/git-tripwire-operations.txt`,
  `docs/development-workflow.txt`, `docs/installation.txt`,
  `docs/running-tests.txt`, and `project-management/git-flow.txt`, and added
  specification
  `docs/specifications/security_entropy_tripwire_git_workflow.txt`.
  Validation: wrapper-based `black`, `ruff`, `compileall`, `entropy_check`,
  `entropy_tripwire_verify`, and `pytest` all passed (`241 passed`).

* Added entropy-check security harness, managed pre-commit hook installer,
  and setup/documentation wiring. Recorded at 2026-03-08T01:00:39-08:00 by
  Codex (GPT-5) from direct operator instruction. Added
  `dev-utils/security/entropy-check.py`,
  `dev-utils/security/run_entropy_harness.py`, and
  `scripts/install_git_hooks.py`; wired setup flow through
  `scripts/install_prerequisites.py` and `scripts/tool_timeouts.json` key
  `entropy_check`; and documented procedures in
  `docs/security/entropy-checks.txt`, `docs/installation.txt`,
  `docs/development-workflow.txt`, and `project-management/git-flow.txt`.
  Added regression coverage in `tests/test_entropy_check.py`,
  `tests/test_entropy_harness.py`, and `tests/test_install_git_hooks.py`.
  Validation: wrapper-based `black`, `ruff`, `compileall`, and `pytest`
  all passed (`231 passed`), and harness run on repository root reports
  zero findings after intentional-test exclusions.

* Added an AI-to-human request queue and standing maintenance orders.
  Recorded at 2026-03-07T22:31:55-08:00 by Codex (GPT-5) from direct
  operator instruction. Added
  `/project-management/state/ai-human-requests.txt` with
  pending/active/completed sections plus formatting guidance, and updated
  `AGENTS.md` and
  `docs/human-orders-for-AI-agents.txt` so agents log requests as pending,
  help reconcile stale states, and ask for human confirmation when possible
  before auto-closing likely completed entries.
  Validation: wrapper-based `black`, `ruff`, `compileall`, and `pytest`
  all passed (`229 passed`).

* Implemented and documented layered Linux-side Windows shim validation.
  Recorded at 2026-03-07T22:26:43-08:00 by Codex (GPT-5) from direct
  operator instruction. Added
  `scripts/windows/verify-windows-shims-linux.sh` as the orchestrator for
  Podman+Wine preflight and host-Wine fallback, plus
  `docs/testing/windows/` runbook, maintenance, and troubleshooting docs.
  Added `docs/specifications/testing/windows_shim_validation_from_linux.txt`,
  updated AGENTS standing orders and AI operator command guidance, and added
  timeout-wrapper tool key `windows_shims_linux`.
  Validation: `black`, `ruff`, `compileall`, `pytest` all passed via timeout
  wrapper, and `windows_shims_linux` executed with expected `SKIP` output for
  host policy/no-Wine conditions.

* Updated timeout management for static-analysis tooling after repeated black
  bailout cutoffs. Recorded at 2026-03-06T22:43:04-08:00 by Codex (GPT-5).
  Updated ``scripts/tool_timeouts.json`` so black runs with ``--no-cache`` and
  a 120-second bailout window before retry. Verified formatter stability on
  all touched Python files, and re-ran wrapper-based ruff, compileall, and
  pytest.

* Completed proposal-status reconciliation for core architecture proposals and
  closed remaining internal-viewer implementation gaps. Recorded at
  2026-03-06T22:43:04-08:00 by Codex (GPT-5). Marked
  ``internal-document-object-model``, ``three-pass-intake-parser``, and
  ``internal-viewer-engine`` as implemented with decision-log history.
  Removed external pager execution paths from ``src/mdview/rendering.py`` and
  ``src/mdview/cli.py``, added internal horizontal panning bindings, and
  updated fallback/prerequisite behavior.
  Validation: repeated full-suite runs via
  ``python scripts/run_tool_with_timeout.py pytest`` (pass; ``226 passed``).

* Implemented Windows 11 command-line support per
  ``project-management/state/proposals/completed/windows-11-cli-support.txt``.
  Recorded at
  2026-03-06T22:43:04-08:00 by Codex (GPT-5). Added PowerShell and cmd.exe
  shims under ``scripts/windows`` for bootstrap and timeout-wrapper checks,
  extended ``scripts/install_prerequisites.py`` for Windows bootstrap flow,
  added ``.github/workflows/windows-cli.yml`` for CI coverage, and updated
  Windows deployment/testing documentation.
  Validation: ``python scripts/run_tool_with_timeout.py pytest`` (pass).

* Drafted ``docs/specifications/flatten_unicode.txt`` for
  ``--flatten-unicode`` behavior. Recorded at 2026-03-06T22:43:04-08:00 by
  Codex (GPT-5) while clearing the related backlog item. The specification
  defines deterministic Unicode-to-ASCII-compatible mappings, reflow-width
  interaction, markdown/link handling, and testing guidance.

* Drafted ``docs/specifications/7bit_mode.txt`` for ``--7bit`` behavior.
  Recorded at 2026-03-06T22:43:04-08:00 by Codex (GPT-5) while clearing the
  related backlog item. The specification defines printable 7-bit enforcement,
  8-bit mapping rules, and ordered combined behavior with
  ``--flatten-unicode``.

* Drafted the multi-version maintenance workflow proposal at
  ``project-management/state/proposals/``
  ``version-management-multi-version-support.txt``.
  Recorded at 2026-03-06T22:43:04-08:00 by Codex (GPT-5) while clearing the
  low-priority backlog item. The proposal covers version-line branching,
  backport and cherry-pick policy, bug linkage across versions, and release
  note traceability requirements.

* Drafted docs/specifications/visual_mode_selection.txt covering vi-style
  visual mode selection. Recorded at 2026-03-06T22:19:07-08:00 by Codex
  (GPT-5) while clearing the visual-mode spec backlog item on
  proposals/visual-mode-selection-spec. The specification defines `V` entry,
  arrow-key navigation, `Space` start/end/clear cycling, `Enter` copy-and-exit
  semantics with multi-clipboard expectations, `Escape` exit behavior, and
  wrap/overflow/resize interaction requirements.

* Added Markdown image, table, and escaping pytest coverage and fixture
  corpus. Recorded at 2026-03-06T22:18:13-08:00 by Codex (GPT-5) while
  clearing three backlog items on feature/markdown-format-coverage. Added
  dedicated suites tests/test_markdown_images.py,
  tests/test_markdown_tables.py, and tests/test_markdown_escaping.py plus
  fixture sets under resources/tests/markdown/images and
  resources/tests/markdown/escaping. Updated src/mdview/rendering.py fallback
  image formatting so plain mode surfaces ``alt (url)`` and missing-URL
  placeholders per specification guidance.
  Validation: ``python scripts/run_tool_with_timeout.py pytest --``
  ``tests/test_markdown_images.py tests/test_markdown_tables.py``
  ``tests/test_markdown_escaping.py`` (pass), and full
  ``python scripts/run_tool_with_timeout.py pytest`` reports
  ``223 passed``.

* Cleared the stabilization gate for the previously failing test cluster.
  Recorded at 2026-03-06T22:13:44-08:00 by Codex (GPT-5) on
  feature/viewer-policy-dom-wiring. Resolved Rich markdown spacing drift,
  removed visible forced-break sentinel leakage, and stabilized plain-text
  default no-reflow behavior under the new CLI policy controls.
  Validation: ``python scripts/run_tool_with_timeout.py pytest`` now reports
  ``207 passed`` with no failing tests.

* Wired viewer policy and DOM contracts into rendering interactions and added
  viewer integration tests. Recorded at 2026-03-06T22:11:52-08:00 by Codex
  (GPT-5) while clearing two viewer backlog items on
  feature/viewer-policy-dom-wiring. Added src/mdview/viewer.py with
  block-level reflow eligibility using hard limits and soft hints, document
  materialization, and ViewerSession dual-axis navigation helpers built on
  ViewportState. Updated src/mdview/rendering.py to consume the shared
  materialization path for plain-text policy rendering.
  Added tests/test_viewer.py plus wide fixtures under resources/tests for
  literal and Markdown table overflow scenarios, panning bounds, and resize
  anchor preservation.
  Validation: ``python scripts/run_tool_with_timeout.py pytest --``
  ``tests/test_viewport.py tests/test_viewer.py tests/test_rendering.py``
  (pass), ``python scripts/run_tool_with_timeout.py ruff --``
  ``src/mdview/viewer.py src/mdview/rendering.py tests/test_viewer.py``
  (pass), and full ``python scripts/run_tool_with_timeout.py pytest`` remains
  at the known 17-failure stabilization cluster.

* Implemented the internal viewer core viewport engine baseline. Recorded at
  2026-03-06T22:09:16-08:00 by Codex (GPT-5) while clearing the top backlog
  item on feature/viewer-core-viewport-engine. Added src/mdview/viewport.py
  with dual-axis viewport state, overflow activation checks, bounded panning,
  resize anchor preservation, and visible-range helpers. Added
  tests/test_viewport.py with isolated coverage for clamping, overflow state,
  pan bounds, resize behavior, and visible slice math.
  Validation: ``python scripts/run_tool_with_timeout.py pytest --``
  ``tests/test_viewport.py`` (pass) and
  ``python scripts/run_tool_with_timeout.py ruff -- src/mdview/viewport.py``
  ``tests/test_viewport.py`` (pass).

* Integrated unified reflow-policy controls into CLI and rendering. Recorded
  at 2026-03-06T22:07:34-08:00 by Codex (GPT-5) while clearing the top
  backlog item on feature/reflow-policy-integration. Added CLI flags
  ``--reflow``, ``--reflow-mode prose|all|none``, and ``--noreflow`` with
  precedence resolution in src/mdview/cli.py and threaded active policy into
  resize rerenders. Updated src/mdview/rendering.py to apply plain-text
  policy behavior, including default no-reflow for ``.txt`` and width-based
  reflow when enabled. Extended tests/test_cli.py and tests/test_rendering.py
  with policy and precedence coverage.
  Validation: ``python scripts/run_tool_with_timeout.py pytest --``
  ``tests/test_cli.py tests/test_rendering.py`` (pass),
  ``python scripts/run_tool_with_timeout.py ruff -- src/mdview/cli.py``
  ``src/mdview/rendering.py tests/test_cli.py tests/test_rendering.py``
  (pass), and full ``python scripts/run_tool_with_timeout.py pytest`` now
  reports 17 failures (down from 18), with
  ``test_render_to_ansi_preserves_five_line_plain_text`` resolved.

* Cleared the three-pass intake parser backlog group and finalized DOM-linked
  parser integration. Recorded at 2026-03-06T21:04:58-08:00 by Codex (GPT-5)
  from direct operator instruction. Implemented pass-1 line analysis, pass-2
  segmentation with full multi-type score vectors, and pass-3 document-level
  score refinement in src/mdview/intake.py, while extending src/mdview/dom.py
  to store line feature vectors and block parser diagnostics. Added mixed
  intake fixtures under resources/tests and added parser regression
  coverage in tests/test_intake_parser.py for Unicode width edges, ambiguity
  handling,
  score-band assertions, deterministic output snapshots, and pass diagnostics.
  Validation: ``python scripts/run_tool_with_timeout.py pytest --``
  ``tests/test_intake_parser.py tests/test_dom.py`` (pass),
  ``python scripts/run_tool_with_timeout.py compileall`` (pass),
  ``python scripts/run_tool_with_timeout.py ruff -- src/mdview/intake.py``
  ``src/mdview/dom.py tests/test_intake_parser.py`` (pass), and full
  ``python scripts/run_tool_with_timeout.py pytest`` still reports the known
  18 pre-existing failures tracked in
  project-management/state/bugs/known-bugs.txt.

* Added DOM unit coverage for invariants and behavior precedence. Recorded at
  2026-03-06T20:58:55-08:00 by Codex (GPT-5) while clearing the top backlog
  item. Expanded tests/test_dom.py to cover frozen-model invariants, source
  fidelity for ingest/export, hard-limit precedence over soft wrap hints, and
  deterministic mixed-content export snapshots.
  Validation: ``python scripts/run_tool_with_timeout.py pytest --``
  ``tests/test_dom.py`` (pass),
  ``python scripts/run_tool_with_timeout.py compileall`` (pass), and
  ``python scripts/run_tool_with_timeout.py ruff -- tests/test_dom.py``
  (pass).

* Added black timeout watchdog retries to the tooling wrapper. Recorded at
  2026-03-06T20:53:07-08:00 by Codex (GPT-5) from direct operator approval.
  Updated scripts/run_tool_with_timeout.py and scripts/tool_timeouts.json so
  black timeout events trigger one cleanup-and-retry cycle before final
  failure. Cleanup now targets only ``python -m black`` processes to avoid
  killing wrapper sessions. Validation: forced-timeout invocation confirmed
  retry and cleanup behavior; ruff/compileall/pytest wrapper checks unchanged.

* Added timeout-enforced tooling wrappers for required local checks. Recorded
  at 2026-03-06T20:40:16-08:00 by Codex (GPT-5) from a direct operator
  request. Added scripts/tool_timeouts.json and
  scripts/run_tool_with_timeout.py so black/ruff/compileall/pytest run with
  short bailout limits by default and explicit timeout overrides when
  justified. Updated AGENTS.md plus docs/development-workflow.txt and
  docs/running-tests.txt to require and document wrapper-based execution.
  Validation: timeout wrapper passed for ruff and compileall; pytest still
  reports the known 18 failing tests.

* Implemented the unified internal document object model baseline from
  project-management/state/proposals/completed/
  internal-document-object-model.txt. Recorded at
  2026-03-07T03:58:54Z by Codex (GPT-5) while clearing the top backlog item.
  Added src/mdview/dom.py with Document/Block/Line and constraint/style
  profiles, added src/mdview/intake.py ingestion helpers, and routed
  src/mdview/rendering.py through intake so rendering consumes the common
  model without source-fidelity regressions. Added smoke coverage in
  tests/test_dom.py and intake-consumption coverage in
  tests/test_rendering.py.
  Validation: black/ruff/compileall passed; pytest remains blocked by the
  known 18-failure cluster tracked in
  project-management/state/bugs/known-bugs.txt.

* Populated project-management/state/bugs/known-bugs.txt with actionable
  entries
  for the current failing-test cluster and linked them back to the backlog
  stabilization gate (BUG-2026-03-07-01 through BUG-2026-03-07-03). Recorded
  at 2026-03-07T03:42:51Z by Codex (GPT-5) while clearing the top backlog
  item. Updated project-management/state/backlog.txt to include
  related-bug links in
  the stabilization gate task. Blockers: none.

* Drafted docs/specifications/reflow_mode.txt as the dedicated
  ``--reflow-mode`` specification defining ``prose|all|none`` semantics,
  precedence with ``--reflow``/``--noreflow``, source defaults, hard-limit
  overrides, and testing guidance. Recorded at 2026-03-07T03:40:50Z by
  Codex (GPT-5) while clearing the top backlog item. Updated
  docs/specifications/project_overview.txt and
  docs/specifications/architecture_and_testing.txt to cross-reference the new
  spec, and aligned docs/specifications/plain_text_reflow_behavior.txt.
  Blockers: none.

* Added ``--verify-resize-detection`` CLI workflow plus a resize verification
  harness with simulator-driven tests and README documentation. Recorded at
  2025-12-12T02:49:55Z by R. Talon for Matt "Breakpoint" Heck.

* Added pytest coverage for Markdown link rendering per the specification,
  covering text visibility, hyperlink metadata, adjacency handling,
  emphasized link boundaries, multiple destinations, and fallback URL
  display. Recorded at 2025-12-11T07:58:07Z by R. Talon while clearing the
  top backlog item.

* Added pytest coverage for Markdown horizontal rule rendering per the
  specification, covering ``---``/``***`` recognition, spacing variants,
  trailing-text suppression, and fallback notices. Recorded at
  2025-12-11T07:42:24Z by R. Talon while clearing the top backlog item.

* Added pytest coverage for Markdown nested list rendering per the
  specification, covering bulleted hierarchies, mixed markers, spacing,
  and wrapped items across Rich and plain fallbacks. Recorded at
  2025-12-11T07:30:25Z by R. Talon while clearing the top backlog item.

* Added pytest coverage for Markdown ordered list rendering per the
  specification, with fixtures for incrementing, offset starts, wrapped
  content, restarts, and wide-number alignment across Rich and plain
  fallbacks. Recorded at 2025-12-11T07:13:16Z by R. Talon while clearing the
  top backlog item.

* Added pytest coverage for Markdown bulleted list rendering per the
  specification, including fixtures for hyphen, asterisk, mixed markers, and
  spacing preservation across Rich and plain fallbacks. Recorded at
  2025-12-11T08:10:00Z by R. Talon while clearing the top backlog item.

* Added pytest coverage for Markdown blockquote rendering per the
  specification, introducing fixtures for basic, nested, emphasis, spacing,
  and multi-line quotes. Recorded at 2025-12-11T06:50:38Z by R. Talon while
  clearing the top backlog item.

* Added pytest coverage for Markdown fenced code block rendering per the
  specification, introducing fixtures for basic fences, language hints,
  whitespace preservation, and fallback warnings. Recorded at
  2025-12-11T06:37:10Z by R. Talon while clearing the top backlog item.

* Added pytest coverage for Markdown inline code rendering per the
  specification, including fixtures for spacing, punctuation, escaped
  backticks, boundaries, and mixed line endings across Rich and plain
  fallbacks. Recorded at 2025-12-11T06:27:01+00:00 by R. Talon while clearing
  the top backlog item.

* Added pytest coverage for Markdown strikethrough rendering per the
  specification, with fixtures for adjacency to emphasis, escape handling,
  punctuation spacing, unterminated markers, and mixed line endings across
  Rich and plain fallbacks. Recorded at 2025-12-11T06:11:34Z by R. Talon while
  clearing the top backlog item.

* Added pytest coverage for combined Markdown bold+italic rendering per the
  specification. Included fixtures for triple-marker basics, boundaries,
  unterminated markers, escape handling, and mixed line endings across Rich
  and plain fallbacks. Recorded at 2025-12-11T05:49:19+00:00 by R. Talon while
  clearing the top backlog item.

* Added pytest coverage for Markdown italic rendering per the testing
  specification, with fixtures for marker variants, adjacency, nested bold,
  punctuation, unmatched markers, and mixed line endings. Recorded at
  2025-12-11T05:20:44Z by R. Talon while clearing the top backlog item.

* Added pytest coverage for Markdown bold rendering per the testing
  specification, including fixtures for marker variants, adjacency, nested
  italic content, punctuation handling, unmatched markers, and mixed line
  endings. Recorded at 2025-12-11T05:05:15Z by R. Talon while clearing the
  top backlog item.

* Added pytest coverage for Markdown heading rendering per the testing
  specification, including fixtures for hierarchy, punctuation, inline
  formatting, adjacent text, indented markers, and empty headings.
  Recorded at 2025-12-11T04:56:30Z by R. Talon while clearing the top backlog
  item.

* Drafted docs/specifications/markdown_escaping.txt detailing backslash escape
  rules, formatting isolation, fallback behavior, and fixture inventory.
  Recorded at 2025-12-11T04:45:16Z by R. Talon while clearing the top backlog
  item.

* Drafted docs/specifications/markdown_image_rendering.txt outlining alt text
  visibility, URL exposure, fallbacks for non-rendered images, adjacency
  isolation, and fixture inventory. Recorded at 2025-12-11T04:32:54Z by
  R. Talon while clearing the top backlog item.

* Drafted docs/specifications/markdown_link_rendering.txt covering inline link
  fixtures, link text plus URL surfacing, and renderer-dependent metadata
  handling. Recorded at 2025-12-11T04:22:48Z by R. Talon while clearing the
  top backlog item.

* Drafted docs/specifications/markdown_horizontal_rule_rendering.txt covering
  recognition of ``---`` and ``***`` markers, separator layout, trailing text
  suppression, and fallback cues. Recorded at 2025-12-10T07:55:59Z by R. Talon
  while clearing the top backlog item.

* Drafted docs/specifications/markdown_nested_list_rendering.txt detailing
  parent-child indentation rules, separation requirements, and safeguards
  against level merging. Recorded at 2025-12-10T07:05:14Z by R. Talon while
  clearing the top backlog item.

* Drafted docs/specifications/markdown_ordered_list_rendering.txt covering
  fixtures for numbered lists, continuation alignment, spacing, and
  restart separation. Recorded at 2025-12-09T07:56:16Z by R. Talon while
  clearing the top backlog item.

* Drafted docs/specifications/markdown_bulleted_list_rendering.txt outlining
  fixtures for hyphen and asterisk bullets, spacing expectations, and item
  separation rules. Recorded at 2025-12-09T07:46:36Z by R. Talon while
  clearing the top backlog item.

* Drafted docs/specifications/markdown_blockquote_rendering.txt detailing
  fixture structure, quote styling cues, nesting behavior, interior emphasis
  tolerance, and exit criteria. Recorded at 2025-12-09T07:42:09Z by R. Talon
  while clearing the top backlog item.

* Drafted docs/specifications/markdown_fenced_code_block_rendering.txt
  capturing fixture layout, whitespace preservation, language-hint handling,
  and fallback warnings for missing syntax highlighting. Recorded at
  2025-12-09T07:28:58Z by R. Talon while clearing the top backlog item.

* Refined docs/specifications/markdown_inline_code_rendering.txt with explicit
  fixture formats, punctuation and spacing preservation rules, and fallback
  warnings for missing monospace rendering. Recorded at
  2025-12-09T07:18:00Z by R. Talon while clearing the top backlog item.

* Drafted docs/specifications/markdown_inline_code_rendering.txt defining
  fixtures, monospace styling expectations, punctuation preservation, and
  fallback behavior for inline code spans. Recorded at
  2025-12-09T06:15:06+00:00 by R. Talon while clearing the top backlog item.

* Drafted docs/specifications/markdown_strikethrough_rendering.txt detailing
  double-tilde recognition, strike styling cues, adjacency isolation, escape
  handling, and fallback behavior. Recorded at 2025-12-09T05:59:26Z by
  R. Talon while clearing the top backlog item.

* Completed runtime documentation improvements for argparse help path and
  cross-platform deployment assets. Recorded at 2025-12-09T05:28:36Z by
  R. Talon. Added a parser helper with wrapped ``--help`` output, shared POSIX
  help text, Linux/macOS man pages, Windows guidance, and a supporting
  specification with tests.

* Drafted docs/specifications/markdown_bold_italic_rendering.txt detailing
  combined emphasis rules, fixture layout, dual-style cues, and boundary
  handling expectations. Recorded at 2025-12-09T05:06:54+00:00 by R. Talon.
  Captured Rich and fallback behavior, escape handling, and non-leaking
  adjacency rules.

* Drafted docs/specifications/csv_document_viewing.txt defining CSV-to-
  Markdown table rendering, alignment options, alternating row styling, and
  fallbacks. Recorded at 2025-12-09T05:02:42Z by R. Talon. Documented
  parsing rules, error handling, and test coverage for CSV fixtures.

* Implemented Markdown table rendering support following
  docs/specifications/markdown_table_rendering.txt. Recorded at
  2025-12-09T03:32:49Z by R. Talon. Added pipe-table alignment formatting for
  Rich and fallback rendering, preserved fenced code blocks, and introduced
  pytest coverage with shared fixtures for alignment cues.

* Drafted docs/specifications/testing/markdown_italic_rendering.txt covering
  fixtures, rendering expectations, and spillover containment for Markdown
  italics. Recorded at 2025-12-09T03:21:30+00:00 by R. Talon. Documented
  Rich and fallback cues plus Python 3.7 constraints.

* Drafted docs/specifications/testing/markdown_bold_rendering.txt covering
  fixtures, rendering expectations, and edge cases for Markdown bold emphasis.
  Recorded at 2025-12-09T03:15:33+00:00 by R. Talon. Captured Rich and
  fallback behaviors plus Python 3.7 constraints.

* Drafted docs/specifications/testing/markdown_heading_rendering.txt for
  isolated Markdown heading rendering. Recorded at 2025-12-09T03:10:59+00:00
  by R. Talon. Captured fixtures, rendering cues, fallback expectations, and
  coverage goals without altering other Markdown elements.

* Implemented hyperlink highlighting and cyclic keyboard navigation using the
  prompt_toolkit-driven pager specified in
  docs/specifications/hyperlink_navigation.txt. Recorded at
  2025-12-09T02:51:53+00:00 by R. Talon. Delivered viewport-aware TAB and
  SHIFT+TAB focus traversal, underline plus reverse-video focus styling, and
  fallbacks when prompt_toolkit or a TTY is unavailable.

* Added docs/specifications/focus_aware_pager.txt to select a prompt_toolkit-
  based pager and unblock hyperlink navigation work. Recorded at
  2025-12-09T02:45:23+00:00 by R. Talon. Captured rendering model, focus
  controller design, fallback rules, and test coverage expectations.

* Added the mode-switching hotkey implementation described in
  docs/specifications/mode_switching_hotkey.txt. Recorded at
  2025-12-09T02:40:05+00:00 by R. Talon. Delivered wrap versus horizontal
  toggle state handling, accessibility announcements, preserved search
  anchors, and unit coverage for disabled contexts and offset restoration.

* Implemented the user preferences loader specified in
  docs/specifications/user_preferences_format.txt. Recorded at
  2025-12-09T02:33:53+00:00 by R. Talon. Delivered configuration parsing, user
  config creation from the stock skeleton, and pytest coverage for merge
  precedence and validation.
