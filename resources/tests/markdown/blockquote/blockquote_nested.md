> Parent quote level maintains prefix visibility.
>> Child quote text stays nested with its own marker.
> Parent continues after child to check separation.
Normal text after the quotes returns to baseline.
