This line has an **unclosed bold marker that should stay literal.
Another line starts __bold but never closes, remaining plain.
A closing marker without an opener** must render as text.
Mixed **marker__ pairs stay literal without styling.
