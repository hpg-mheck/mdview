Check code fences with hints and mixed markers.

```python
def greet(name):
    return f"Hello, {name}!"
```

~~~bash
echo "shell run"
ls -la
~~~

```brainfk
++>--.
```

Trailing text confirms unknown hints still render.
