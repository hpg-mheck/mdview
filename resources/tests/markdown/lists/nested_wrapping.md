Opening paragraph before wrapped nesting.

- Parent item contains text that wraps across
  multiple lines so indentation stays aligned with the parent depth.
  - Child item contains wrapped text that
    continues onto another line while keeping child indentation.
  - Second child stays at the same depth after the wrapped sibling.
- Final parent returns to depth one without inheriting child indentation.

Closing paragraph after wrapped nesting.
